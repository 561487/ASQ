# 论文与当前仓库的对应关系

本文依据作者提供的论文稿件 **Adaptive Sparse Q-Matrix Learning for Cognitive Diagnosis** 整理。论文方法简称 **ASQ（自适应稀疏 Q 矩阵学习）**；`DynamicQ` 是当前代码类名。文中的论文结果属于稿件报告，不代表本次已重跑或独立核验。

## 方法公式与代码

沿用论文符号：N 为学生数，M 为试题数，K 为知识点数，d 为嵌入维度。

| 论文部分 | 论文定义 | 当前代码对应 |
| --- | --- | --- |
| §3.2 嵌入 | Eᵉ ∈ R^(M×d)，Eᵏ ∈ R^(K×d)，随机初始化并学习 | `model.py` 的 `item_embed`、`skill_embed` |
| §3.2 投影 | ẽ = Wₑe+bₑ，k̃ = Wₖk+bₖ | `dynamic_q.py` 的 `item_proj`、`skill_proj` |
| §3.2 连续关联 | Z[j,l] = ẽⱼᵀk̃ₗ / τ | `logits`；Z 为实数关联分数，不是归一化概率 |
| §3.3 稀疏映射 | Q* = Entmax₁.₅(Z)，沿知识点维度计算 | 安装 entmax 时的默认映射分支 |
| §3.3 归一化 | Q_hat[j,l] = Q*[j,l] / (sum_l Q*[j,l] + ε) | ε=1e-8；不启用先验融合时返回的 `Q_star` 对应论文 Q_hat |
| §3.4 预测 | r_hat = f_Θ(sᵢ,eⱼ,Q_hat[j,:]) | 取题目行，再进入当前 NCDM 骨干（含知识点注意力） |
| §3.4 优化 | 对训练交互求 BCE，联合优化 Φ 与 Θ | `train.py` 的 BCELoss、反向传播及 Adam |

Q 由嵌入和投影参数生成，其变化来自这些参数更新；不是独立优化每个 Q 元素。所有学生共享同一 Q，跨模型实验指各下游模型分别训练 ASQ，不表示一份 Q 直接跨模型迁移。

## 关键差异

| 项目 | 提供的论文稿件 | 当前仓库 |
| --- | --- | --- |
| 缩放参数 | §5.4 最终采用 τ=1 | `DynamicQ` 默认 τ=0.7，Net 和命令行未暴露入口 |
| 稀疏映射 | Entmax₁.₅ | 缺依赖时回退 softmax；另有 sparsemax 实现，均不能当作论文 Entmax 主实验 |
| 专家先验 | 主方法不包含专家 Q 融合 | `--use_q_init` 每次前向融合 10% 专家先验，是额外扩展 |
| NCDM 骨干 | 同一模型在不同 Q 来源下保持主体设置一致，参考原始模型设置 | 已加入注意力、LayerNorm、GELU、温度参数；与论文实验所用 NCDM 版本尚待对应 |
| 数据 | 三个数据集及指定预处理统计 | 仅 ASSIST09 来源数据，配置和过滤规则与论文不同 |
| 五折抽样 | 全部交互随机分成五份 | 每个学生内部打乱后轮流分到五折，属于另一种交互级划分 |
| 模型选择 | 测试不参与训练与参数更新，未明确验证划分/选轮次规则 | 验证复用测试，按其 AUC 选最优轮次，测试参与了模型选择 |
| 对比方法 | Expert Q、BMF-Q、QCCDM-aug Q、ASQ | 静态标注和 ASQ 核心模块；缺少 BMF-Q 与 QCCDM-aug Q |
| 下游模型 | NCDM、RCD、CDMFKC、KaNCD | 仅当前修改版 NCDM |
| RQ2 | Expert Q / Continuous Q / ASQ | 缺少独立且定义明确的 Continuous Q 模式 |
| RQ3 | 平均每题权重大于 1/K 的有效关联数 | 缺少导出及统计入口 |
| RQ4 | τ∈{0.1,0.5,1,2,5,10}，5 个种子，AUC/ACC 均值与标准差 | 缺少 τ 命令行参数、统一种子控制及实验汇总流程 |

## 数据口径

| 数据集 | 论文学生数 | 论文试题数 | 论文知识点数 | 论文交互数 |
| --- | ---: | ---: | ---: | ---: |
| ASSIST09 | 3,079 | 17,671 | 123 | 271,421 |
| ASSIST17 | 1,709 | 3,162 | 102 | 942,814 |
| Junyi | 10,000 | 835 | 835 | 353,835 |

论文 ASSIST09 描述去除无效答题时间、少于 10 道作答的学生和出现少于 30 次的题目。仓库 `config.txt` 则为 4163/17746/123，划分脚本过滤少于 15 条作答的学生，缺少论文所述完整原始数据处理流程。不能仅修改配置数字就认为数据已与论文一致。

稿件数据口径也需进一步核实：如果 ASSIST09 的 17,671 道题均至少出现 30 次，交互数至少应为 530,130，与表中的 271,421 不符。需明确该阈值是否施加于更早处理阶段、之后是否还有筛选，或是否为文字/统计错误。本文保留论文报告值，不擅自更改数据。

## 复现前需补齐的证据

需要与论文实际实验对应的数据预处理版本和划分、NCDM 骨干版本、完整超参数、每折结果及检查点。Continuous Q 的非负权重生成方式、五折验证/检查点选择规则，以及 RQ3 对哪些下游模型或折的 Q 进行汇总，也需在最终实验说明中明确。

本次文档更新对齐论文定位与已报告内容，不通过改写文字宣称代码已经完成上述差异项。
