# 实验设置与报告边界

## 目标与对照

核心问题是在 NCDM 中引入本项目学习 Q 方法是否有效。以下是实验组织方案，不代表实验已经完成。

| 对照组 | 用途 | 状态 |
| --- | --- | --- |
| 原版 NCDM + 静态 Q | 基础模型参照 | 尚无独立原版实现 |
| 当前修改骨干 + 静态 Q | 控制骨干修改的影响 | 已实现 |
| 当前修改骨干 + 学习 Q | 相同骨干下评估 Q 替换 | 已实现 |
| 当前修改骨干 + 学习 Q + 专家先验 | 评估持续融合专家先验 | 已实现 |

命令见 [README](README.md) 和 [DynamicQ 说明](DYNAMICQ_USAGE.md)。静态输入为多热向量，学习 Q 在注意力处理前归一化，因此比较同时涉及关联结构与尺度变化。可补充归一化静态 Q、映射类型、融合权重消融以分离影响；这些尚非现有命令行功能。

## 数据划分现状

| 数据或脚本 | 核查结果 | 使用要求 |
| --- | --- | --- |
| 自带根目录验证集与测试集 | SHA-256 相同 | 重新生成独立划分 |
| `divide_data.py` | 学生内部约 70%/10%/20%，过滤少于 15 条作答的学生 | 未固定种子，各模型复用同一划分 |
| `kfold_divide_data.py` | 学生内部打乱后轮流分配到五折，当前折测试、其余训练，验证复用测试 | 从非测试部分另划验证集 |
| `kfold_run.py` | 按验证 AUC 选最佳模型再测试 | 当前与上述划分组合会让测试数据参与模型选择 |

五折划分种子为 2025；训练脚本未统一设置 Python、NumPy 和 PyTorch 随机种子，不能据此称训练完全可复现。

五折是学生内部作答日志划分，不是按学生互斥的冷启动评估。修正后的协议应保留独立测试部分，从其余部分划出验证集，只用验证集选检查点。旧划分结果需要重跑，修改说明文字不能使其成为独立测试结果。

## 现有五折接口

以下记录现有接口，供维护时核对。当前协议有上述问题，不能直接用于最终论文结论。

```bash
python kfold_divide_data.py
python kfold_run.py cpu 70 --use_dynamic_q --d_model 128 --patience 5
python analyze_kfold_results.py
```

运行前需创建 `model/` 和 `result/`，具体命令见 README。五折运行会逐折覆盖根目录划分，结束后不恢复原数据；检查点也被后续折覆盖。测试日志追加到同一文件，未记录折号、模式或种子。

分析脚本汇总所有匹配日志行，不检查是否恰好五折，也不区分配置或多次运行。标准差采用 NumPy 默认的总体标准差（`ddof=0`）。只有输入明确属于同一配置、同一协议的完整结果时，汇总才有明确含义。

## 报告要求

每个配置和种子独立保存划分标识、代码与依赖版本、实际 Q 映射、嵌入维度、专家先验设置、轮数、早停设置、最佳轮次及检查点。各配置复用相同划分，测试不参与选轮次或超参数。

预测指标报告 AUC、RMSE、Accuracy；多折或多种子结果注明统计单位、重复次数及标准差定义。仓库当前两个结果日志为空，不填入示例数值或未经验证的提升结论。

若主张 Q 稀疏性、结构准确性或解释性改善，还需保存 Q，测量零值比例、每题非零知识点数、与可用标注的对应关系，并分析案例。无专家先验模式的列是否对应原知识点语义，需要独立验证。
